{
  "name": "ai-meeting-transcription",
  "version": "1.0.0",
  "description": "AI-powered meeting transcription and analysis platform with React frontend and FastAPI backend",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "start": "npm run dev",
    "frontend": "cd frontend && npm run dev",
    "backend": "source .venv/bin/activate && cd backend && python ffmpeg_free_main.py",
    "build": "cd frontend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && pip install -r requirements.txt",
    "clean": "rm -rf frontend/dist frontend/.vite frontend/node_modules/.vite && cd frontend && npm run clean",
    "lint": "cd frontend && npm run lint",
    "preview": "cd frontend && npm run preview",
    "test:backend": "cd backend && python test_mistral.py && python test_direct_summary.py",
    "reprocess": "source .venv/bin/activate && cd backend && python reprocess_existing.py",
    "setup": "npm run install:all && echo '✅ Setup complete! Run npm run dev to start the application'"
  },
  "keywords": [
    "ai",
    "transcription",
    "meeting",
    "speech-to-text",
    "whisper",
    "mistral",
    "react",
    "fastapi",
    "typescript",
    "python"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/witarsana/frontend-ai.git"
  },
  "bugs": {
    "url": "https://github.com/witarsana/frontend-ai/issues"
  },
  "homepage": "https://github.com/witarsana/frontend-ai#readme"
}
